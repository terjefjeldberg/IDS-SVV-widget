services:
  - type: web
    name: ids-svv-ifctester
    runtime: python
    rootDir: ifctester_service
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 300
    healthCheckPath: /health
